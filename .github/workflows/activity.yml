name: Legacy Activity Bot (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: echo "Legacy bulk-commit workflow is disabled. Use keep-active.yml instead."
